name: YouTube Shorts Automation

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * *"
    - cron: "0 13 * * *"
    - cron: "0 17 * * *"
    - cron: "0 21 * * *"

permissions:
  contents: read

jobs:
  create-and-upload-short:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
      YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
      YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install required tools
        shell: bash
        run: |
          set -euo pipefail

          sudo apt-get update
          sudo apt-get install -y ffmpeg jq python3-pip curl unzip

          python3 -m pip install --upgrade pip

          python3 -m pip install \
            google-api-python-client \
            google-auth \
            google-auth-oauthlib \
            google-auth-httplib2

      - name: Check required secrets
        shell: bash
        run: |
          set -euo pipefail

          missing=0

          for name in \
            GEMINI_API_KEY \
            PEXELS_API_KEY \
            YOUTUBE_CLIENT_ID \
            YOUTUBE_CLIENT_SECRET \
            YOUTUBE_REFRESH_TOKEN
          do
            if [ -z "${!name:-}" ]; then
              echo "::error::$name is missing from GitHub Secrets."
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

          echo "All required secrets are available."

      - name: Generate Arabic Short with Gemini
        shell: bash
        run: |
          set -euo pipefail

          python3 - <<'PY'
          import json
          import os
          import time
          import urllib.error
          import urllib.request

          api_key = os.environ.get("GEMINI_API_KEY", "").strip()

          if not api_key:
              raise RuntimeError("GEMINI_API_KEY is empty")

          model = "gemini-3.6-flash"

          url = (
              "https://generativelanguage.googleapis.com/"
              "v1beta/models/"
              + model +
              ":generateContent"
          )

          scene_schema = {
              "type": "object",
              "properties": {
                  "text": {
                      "type": "string"
                  },
                  "pexels_query": {
                      "type": "string"
                  }
              },
              "required": [
                  "text",
                  "pexels_query"
              ]
          }

          schema = {
              "type": "object",
              "properties": {
                  "hook": {
                      "type": "string"
                  },
                  "script": {
                      "type": "string"
                  },
                  "title": {
                      "type": "string"
                  },
                  "description": {
                      "type": "string"
                  },
                  "tags": {
                      "type": "array",
                      "items": {
                          "type": "string"
                      }
                  },
                  "scenes": {
                      "type": "array",
                      "items": scene_schema
                  },
                  "voice": {
                      "type": "string"
                  }
              },
              "required": [
                  "hook",
                  "script",
                  "title",
                  "description",
                  "tags",
                  "scenes",
                  "voice"
              ]
          }

          prompt = """
          أنت كاتب محترف لفيديوهات YouTube Shorts العربية.

          أنشئ فيديو واحدًا فقط عن حقيقة مدهشة وصحيحة وقابلة للتحقق.

          الهدف:
          فيديو سريع وممتع يجعل المشاهد يريد إكماله حتى النهاية.

          اللغة:
          عربية طبيعية جدًا.
          لا تكتب بأسلوب مقال أو كتاب.
          استخدم جمل قصيرة تناسب التعليق الصوتي.
          لا تستخدم رموزًا تعبيرية.
          لا تستخدم هاشتاغات داخل السكربت.

          HOOK:
          جملة واحدة فقط.
          قوية ومثيرة للفضول.
          لا تبدأ بـ:
          "هل تعلم أن..."
          "في هذا الفيديو..."
          "اليوم سوف نتحدث..."
          "هل تعلم؟"

          SCRIPT:
          بين 45 و70 كلمة فقط.
          ابدأ بالـhook.
          قدم حقيقة واحدة مدهشة.
          اجعل كل جملة تضيف معلومة جديدة.
          لا تكرر الفكرة نفسها.
          لا تستخدم مقدمة طويلة.
          لا تستخدم حشوًا.
          النهاية تكون سؤالًا قصيرًا أو جملة تشجع على المتابعة.

          SCENES:
          قسّم الفيديو إلى 4 إلى 6 مشاهد.
          كل مشهد يحتوي على جملة أو جملتين من السكربت.
          لكل مشهد أعطِ كلمة أو كلمتين بالإنجليزية فقط للبحث في Pexels.
          يجب أن يكون البحث مرتبطًا بما يظهر في المشهد فعلًا.

          أمثلة:
          space
          astronaut
          deep ocean
          human brain
          volcano
          wild animal
          ancient egypt
          robot technology

          لا تستخدم كلمات عامة مثل:
          abstract
          background
          nature
          random

          TITLE:
          عنوان عربي قصير ومثير للفضول.
          أقل من 90 حرفًا.
          يجب أن ينتهي بـ #Shorts

          DESCRIPTION:
          جملتان عربيتان قصيرتان.
          ثم سطر جديد يحتوي على 5 هاشتاغات عربية أو إنجليزية مناسبة.

          TAGS:
          8 إلى 12 كلمة مفتاحية.
          اجعلها lowercase.

          VOICE:
          يجب أن يكون:
          ar_JO-kareem-medium

          المواضيع:
          العلوم
          الفضاء
          التاريخ
          الحيوانات
          جسم الإنسان
          النفس البشرية
          التكنولوجيا
          الطبيعة
          المحيطات
          الأرض
          الكون

          اختر موضوعًا مختلفًا عن الفيديوهات السابقة قدر الإمكان.

          مهم جدًا:
          لا تختر خرافة.
          لا تختر معلومة غير مؤكدة.
          لا تختر أرقامًا مثيرة للشك.
          إذا كانت المعلومة قابلة للالتباس، اختر حقيقة أخرى.

          أعد JSON فقط.
          """

          body = {
              "contents": [
                  {
                      "role": "user",
                      "parts": [
                          {
                              "text": prompt
                          }
                      ]
                  }
              ],
              "generationConfig": {
                  "temperature": 0.75,
                  "maxOutputTokens": 4000,
                  "responseMimeType": "application/json",
                  "responseSchema": schema
              }
          }

          request = urllib.request.Request(
              url,
              data=json.dumps(
                  body,
                  ensure_ascii=False
              ).encode("utf-8"),
              headers={
                  "x-goog-api-key": api_key,
                  "Content-Type": "application/json"
              },
              method="POST"
          )

          data = None

          for attempt in range(1, 4):

              try:

                  print(
                      "Gemini request attempt:",
                      attempt
                  )

                  with urllib.request.urlopen(
                      request,
                      timeout=90
                  ) as response:

                      result = json.load(response)

                  content = (
                      result["candidates"][0]
                      ["content"]["parts"][0]["text"]
                  )

                  data = json.loads(content)

                  break

              except json.JSONDecodeError as e:

                  print(
                      "Gemini returned invalid JSON."
                  )

                  if attempt < 3:
                      time.sleep(2)
                      continue

                  raise RuntimeError(
                      "Gemini returned invalid JSON after 3 attempts."
                  ) from e

              except urllib.error.HTTPError as e:

                  error_body = e.read().decode(
                      "utf-8",
                      errors="replace"
                  )

                  print(
                      "Gemini HTTP status:",
                      e.code
                  )

                  print(
                      "Gemini response:",
                      error_body
                  )

                  raise

              except (
                  KeyError,
                  IndexError,
                  TypeError
              ) as e:

                  if attempt < 3:
                      time.sleep(2)
                      continue

                  raise RuntimeError(
                      "Unexpected Gemini response."
                  ) from e

          if not isinstance(data, dict):
              raise RuntimeError(
                  "Gemini response is not a JSON object."
              )

          hook = str(
              data.get("hook", "")
          ).strip()

          script = str(
              data.get("script", "")
          ).strip()

          title = str(
              data.get("title", "")
          ).strip()

          description = str(
              data.get("description", "")
          ).strip()

          tags = data.get("tags", [])

          scenes = data.get("scenes", [])

          if not hook:
              raise RuntimeError(
                  "Missing hook."
              )

          if not script:
              raise RuntimeError(
                  "Missing script."
              )

          if not title:
              raise RuntimeError(
                  "Missing title."
              )

          if not isinstance(
              scenes,
              list
          ):
              raise RuntimeError(
                  "Scenes must be a list."
              )

          if len(scenes) < 4:
              raise RuntimeError(
                  "Gemini generated fewer than 4 scenes."
              )

          words = len(script.split())

          if words < 45 or words > 70:
              raise RuntimeError(
                  f"Script must contain 45-70 words, got {words}."
              )

          clean_scenes = []

          for scene in scenes:

              if not isinstance(
                  scene,
                  dict
              ):
                  continue

              text = str(
                  scene.get(
                      "text",
                      ""
                  )
              ).strip()

              pexels_query = str(
                  scene.get(
                      "pexels_query",
                      ""
                  )
              ).strip()

              if text and pexels_query:

                  clean_scenes.append(
                      {
                          "text": text,
                          "pexels_query": pexels_query
                      }
                  )

          if len(clean_scenes) < 4:
              raise RuntimeError(
                  "Not enough valid scenes."
              )

          if not isinstance(
              tags,
              list
          ):
              tags = []

          tags = [
              str(tag).strip().lower()
              for tag in tags
              if str(tag).strip()
          ]

          job = {
              "hook": hook,
              "script": script,
              "title": title,
              "description": description,
              "tags": tags,
              "voice": "ar_JO-kareem-medium",
              "scenes": clean_scenes,
              "queries": [
                  scene["pexels_query"]
                  for scene in clean_scenes
              ],
              "query": clean_scenes[0]["pexels_query"],
              "per": 5
          }

          with open(
              "job.json",
              "w",
              encoding="utf-8"
          ) as file:

              json.dump(
                  job,
                  file,
                  ensure_ascii=False,
                  indent=2
              )

          print(
              "Arabic Short generated successfully."
          )

          print(
              "Title:",
              title
          )

          print(
              "Script words:",
              words
          )

          print(
              "Scenes:",
              len(clean_scenes)
          )

          for index, scene in enumerate(
              clean_scenes,
              1
          ):

              print(
                  f"Scene {index}:",
                  scene["pexels_query"]
              )
          PY

      - name: Verify generated job
        shell: bash
        run: |
          set -euo pipefail

          test -s job.json

          python3 - <<'PY'
          import json

          with open(
              "job.json",
              "r",
              encoding="utf-8"
          ) as file:

              job = json.load(file)

          required = [
              "hook",
              "script",
              "title",
              "description",
              "tags",
              "voice",
              "scenes",
              "queries"
          ]

          for key in required:

              if key not in job:

                  raise RuntimeError(
                      f"job.json missing: {key}"
                  )

          words = len(
              job["script"].split()
          )

          if words < 45 or words > 70:

              raise RuntimeError(
                  f"Invalid script length: {words}"
              )

          if job["voice"] != \
              "ar_JO-kareem-medium":

              raise RuntimeError(
                  "Invalid voice."
              )

          if not isinstance(
              job["scenes"],
              list
          ):

              raise RuntimeError(
                  "Scenes must be a list."
              )

          if len(job["scenes"]) < 4:

              raise RuntimeError(
                  "At least 4 scenes required."
              )

          for scene in job["scenes"]:

              if not scene.get(
                  "text",
                  ""
              ).strip():

                  raise RuntimeError(
                      "Scene text is empty."
                  )

              if not scene.get(
                  "pexels_query",
                  ""
              ).strip():

                  raise RuntimeError(
                      "Scene Pexels query is empty."
                  )

          print(
              "job.json verified successfully."
          )

          print(
              "Words:",
              words
          )

          print(
              "Scenes:",
              len(job["scenes"])
          )
          PY

      - name: Produce video
        shell: bash
        run: |
          set -euo pipefail

          RUN_DIR="$RUNNER_TEMP/short"

          mkdir -p "$RUN_DIR"

          cp job.json "$RUN_DIR/job.json"

          chmod +x scripts/produce.sh

          scripts/produce.sh "$RUN_DIR"

          test -s "$RUN_DIR/video.mp4"

          cp "$RUN_DIR/video.mp4" short.mp4

          echo "Video created successfully."

      - name: Upload Short to YouTube
        shell: bash
        run: |
          set -euo pipefail

          python3 - <<'PY'
          import json
          import os

          from google.oauth2.credentials import Credentials
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaFileUpload

          credentials = Credentials(
              token=None,
              refresh_token=os.environ[
                  "YOUTUBE_REFRESH_TOKEN"
              ],
              token_uri=(
                  "https://oauth2.googleapis.com/token"
              ),
              client_id=os.environ[
                  "YOUTUBE_CLIENT_ID"
              ],
              client_secret=os.environ[
                  "YOUTUBE_CLIENT_SECRET"
              ],
              scopes=[
                  "https://www.googleapis.com/auth/youtube.upload"
              ]
          )

          youtube = build(
              "youtube",
              "v3",
              credentials=credentials
          )

          with open(
              "job.json",
              "r",
              encoding="utf-8"
          ) as file:

              job = json.load(file)

          body = {
              "snippet": {
                  "title": job["title"],
                  "description": job.get(
                      "description",
                      ""
                  ),
                  "tags": job.get(
                      "tags",
                      []
                  ),
                  "categoryId": "27"
              },
              "status": {
                  "privacyStatus": "public",
                  "selfDeclaredMadeForKids": False
              }
          }

          media = MediaFileUpload(
              "short.mp4",
              mimetype="video/mp4",
              resumable=True
          )

          request = youtube.videos().insert(
              part="snippet,status",
              body=body,
              media_body=media
          )

          response = None

          while response is None:

              _, response = request.next_chunk()

          video_id = response["id"]

          print(
              "Uploaded:",
              f"https://www.youtube.com/watch?v={video_id}"
          )
          PY

      - name: Save generated files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: youtube-short-${{ github.run_id }}
          path: |
            short.mp4
            job.json
          if-no-files-found: ignore
          retention-days: 3
